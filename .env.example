# Copy this file to `.env` in the repository root. `apps/landing/vite.config.ts`
# sets `envDir` to the repository root, so the landing build reads it there.

# Public reader URL used by the landing-page CTA. Set this per environment.
# Do not commit a production URL or secrets to this file.
VITE_APP_URL=
