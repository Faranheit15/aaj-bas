description = "Run the repository's merge-blocking validation suite."

prompt = """
Use the shared `check` skill at `.agents/skills/check/SKILL.md`, then follow
`docs/workflows/check.md`. Run the complete check suite and report only commands
that actually ran: {{args}}
"""
