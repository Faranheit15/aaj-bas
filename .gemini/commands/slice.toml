description = "Implement one backlog item as a single vertical slice."

prompt = """
Use the shared `slice` skill at `.agents/skills/slice/SKILL.md`, then follow
`docs/workflows/slice.md` for this task: {{args}}

Keep package and repository checks on the project's Bun-based command
surface.
"""
