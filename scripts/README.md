# Scripts

This directory holds repository check scripts -- currently `check-agents-md-size.sh`, which guards the agent instruction files against silent Codex truncation, and `check-package-manager.sh`, which enforces `AGENTS.md` section 8. It is also reserved for future deterministic content-processing scripts. The repository foundation does not fetch sources, generate summaries, or publish editions.
